# Module 22: Understand LangChain4j for Java

## Video 1: LangChain4j's Declarative AI Services vs Spring AI

| Script (one row per slide) | Guidance for Complex Visuals |
| :--- | :--- |
| The Java ecosystem now has two mature frameworks for integrating language models into production applications: Spring AI and LangChain4j. Spring AI provides a fluent, builder-based API—the `ChatClient`—that follows the same patterns as Spring's `WebClient` or `RestTemplate`. LangChain4j takes a fundamentally different approach: instead of a fluent API you call imperatively, LangChain4j lets you define an AI interaction as a plain Java interface, and the framework generates the implementation at runtime. These are not competing tools—they represent two distinct design philosophies. Understanding both gives you the vocabulary to choose the right one for your team's architecture. In this lesson, we are going to understand LangChain4j's declarative model and how it compares to Spring AI's builder pattern. | |
| The core abstraction in LangChain4j is the `@AiService` interface. You define a standard Java interface, annotate it with `@AiService`, and declare methods with descriptive signatures. The method's return type defines the expected output structure. The prompt is specified using `@SystemMessage` and `@UserMessage` annotations directly on the method. At runtime, LangChain4j generates a proxy implementation that handles prompt construction, LLM invocation, and response parsing—all behind a clean method call. This design pattern should feel familiar—it is the same interface-driven philosophy behind Spring Data repositories and Spring Cloud's `@HttpExchange` declarative clients. You write the contract; the framework writes the implementation. The key advantage is that your business logic never touches prompt strings, HTTP calls, or JSON parsing. It calls a typed method and gets a typed result. | **Complex Visual:** Design a two-layer diagram. Top layer, labeled "What You Write": show a simple card representing a Java interface with three method-shaped horizontal bars inside it (each bar is a different color, representing a different method). The card has a small badge labeled "@AiService" in the top corner. Bottom layer, labeled "What the Framework Generates": show a more complex box with icons inside representing the hidden work — a speech bubble icon (prompt), a brain icon (LLM call), and a clipboard icon (response parsing). Connect the two layers with a downward arrow labeled "Generated at Runtime." The visual should communicate: you write the simple top part, the framework handles the complex bottom part. Note for designers: think of this like ordering from a restaurant menu — you choose from a clean list (the interface), and the kitchen (the framework) handles all the cooking behind the scenes. |
| Where LangChain4j and Spring AI diverge most is their architectural philosophy. Spring AI follows the Spring "portable service abstraction" pattern—it is deeply integrated into Spring Boot's auto-configuration, dependency injection, and Micrometer observability. Your AI code feels like the rest of your Spring application. LangChain4j takes a framework-agnostic approach—it works identically across Spring Boot, Quarkus, Micronaut, and plain Java. It treats AI integration as a standalone library concern, not a framework concern. This distinction matters for your architecture decisions. If your team is fully committed to the Spring ecosystem and values standardized configuration via `application.yml` and native Spring Boot metrics, Spring AI is the natural fit. If your team works across multiple Java frameworks, or if you prefer explicit builder patterns over auto-configuration, LangChain4j gives you maximum portability. Both frameworks support the same critical capabilities—structured output, tool calling, RAG, and conversation memory—so the decision is about architectural fit, not feature gaps. Both also support conversation memory—the ability to maintain context across multiple user turns. The most common strategy is window-based memory, such as `MessageWindowChatMemory`, which retains the last N messages. Alternatives include token-based memory, which stays within a token budget, and summarizing memory, which compresses older exchanges into a condensed summary. Both frameworks also support streaming responses via Server-Sent Events—instead of waiting for the complete response, the model sends tokens incrementally as they are generated, enabling real-time display in the user interface. | |
| Tool calling in LangChain4j follows the same declarative philosophy. You define tool methods annotated with `@Tool` in separate classes—each containing the real implementation logic—and LangChain4j registers them with the model automatically. When the LLM determines a tool is relevant, it invokes the tool, receives the result, and incorporates it into its response. This cycle—reason about the query, act by calling a tool, observe the result, decide whether to continue or respond—is called the agent loop. It turns your `@AiService` from a static text generator into an agent that can interact with live services—querying databases, checking inventory, or calling external APIs. Spring AI 2.0 also uses the `@Tool` annotation for declarative tool registration. In both frameworks, tool methods live in separate classes—not inside the AI service interface. The difference is in registration. In LangChain4j with Spring Boot, `@Tool` beans are auto-discovered and bound to the `@AiService` at build time—registration is implicit. In Spring AI, you explicitly pass tool beans to the `ChatClient` via `.tools()` at the call site, giving you flexibility to use different tool sets for different prompts. Neither approach is objectively better—it is a trade-off between implicit binding and explicit composition. | |
| To summarize: Spring AI and LangChain4j are complementary approaches to the same problem. Spring AI is ecosystem-native—ideal when your application is built entirely on Spring Boot and you want AI to integrate seamlessly with your existing infrastructure. LangChain4j is ecosystem-agnostic—ideal when you need portability, prefer interface-driven design, or want your AI service contracts to be as explicit as your REST contracts. In production, many teams use both: Spring AI for infrastructure-level concerns like vector store integration and observability, and LangChain4j for defining clean, testable AI service interfaces. The skill is knowing which to reach for based on the problem at hand. Thanks for watching, and I'll see you in the next video. | |
