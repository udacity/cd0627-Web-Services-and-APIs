type Query {
    orders: [Order!]!
}

type Mutation {
    createOrder(totalAmount: Float!, customerId: ID!): Order!
}

type Order {
    id: ID!
    totalAmount: Float!
    status: String!
    customer: Customer
}

type Customer {
    id: ID!
    name: String!
    email: String!
}
